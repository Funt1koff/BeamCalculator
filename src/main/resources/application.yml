spring:
  datasource:
    url: jdbc:postgresql://localhost:6552/BeamCalculator
    username: ucd
    password: Qwerty111++
  application:
    name: Beam Calculator
  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 1337